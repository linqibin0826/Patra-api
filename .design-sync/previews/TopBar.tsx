import { TopBar } from "patra-learn";

export const Default = () => (
  <div style={{ width: 720 }}>
    <TopBar />
  </div>
);

export const NarrowViewport = () => (
  <div style={{ width: 560 }}>
    <TopBar />
  </div>
);

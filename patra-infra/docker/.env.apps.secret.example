# 应用容器真敏感密钥模板
# ============================================================
# 复制为 .env.apps.secret（已 gitignore，绝不提交到公开仓库），按需填入真实值。
# compose 的 env_file 会在文件存在时自动叠加加载。
#
# 用途：凡是泄露即事故的密钥都放这里 —— 绝不要写进随仓库提交的 .env.apps。
#
# registry 当前不需要任何真敏感密钥（只连内网 PG + Nacos）；本文件为将来
# ingest 等需要外部数据源 API key 的服务预留示例：
#
# OPENAI_API_KEY=sk-xxxxxxxxxxxx
# PUBMED_API_KEY=xxxxxxxxxxxx
# CROSSREF_API_TOKEN=xxxxxxxxxxxx

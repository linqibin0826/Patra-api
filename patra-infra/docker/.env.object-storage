# object-storage 专属（非敏感 dev 默认值）。共享坐标（Nacos / MinIO）见 .env.common。
# 真敏感密钥写 .env.object-storage.secret（已 gitignore）。

# 数据库（patra-net 服务名 postgres:5432）
STORAGE_DB_URL=jdbc:postgresql://postgres:5432/patra_storage
STORAGE_DB_USERNAME=postgres
STORAGE_DB_PASSWORD=123456

# ============================================================
# patra-learn 容器 - 运行时坐标（非敏感，随仓库提交）
# ============================================================
# learn 是纯静态学习站，无外部服务依赖（不读 .env.common）。

# Next.js standalone server 监听端口（与 compose ports 映射一致）
PORT=4001

NODE_ENV=production
